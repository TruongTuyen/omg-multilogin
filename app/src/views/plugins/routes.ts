const route: RouteConfig[] = [
    {
        key: 'Plugins',
        path: '/plugins',
        createConfig: {
            showTitlebar: true,
            hideMenus: true,
        },
    },
];

export default route;
